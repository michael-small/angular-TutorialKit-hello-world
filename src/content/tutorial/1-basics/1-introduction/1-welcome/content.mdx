---
type: lesson
title: About This Tutorial
focus: /app.component.html
---

# About This Tutorial

Signals are sick

```ts
$number = signal(3) ; // 3
$doubleNumber = computed(() => this.$number() * 2); // 6
$stuff = signal('whoa'); // 'whoa'
$stuffCaps = computed(() => {
    return this.$stuff().toUpperCase(); // WHOA
});
```